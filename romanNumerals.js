function toRomanLazy(num) {
  answer = ''

  const romanNumeralToArabic = {
    'I': 1,
    'V': 5,
    'X': 10,
    'L': 50,
    'C': 100,
    'D': 500,
    'M': 1000
  }

  
  /**
   * while num > 0
   * if num - 1000 > 0, answer += 'M'
   */
  
}

function toRoman(num) {
  return "";
}

module.exports = { toRoman, toRomanLazy };

// testing